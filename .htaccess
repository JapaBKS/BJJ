# Ativa reescrita
RewriteEngine On

# Garante base correta (subpasta /BJJ)
RewriteBase /BJJ/

# ignora arquivos/pastas existentes
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# tudo vai para index.php
RewriteRule ^ index.php [L]
